'use strict';

const textName = document.getElementById('city-name-txt');
const textID = document.getElementById('city-id-txt');

const radioId = document.getElementById('city-id-rbtn');
const radioName = document.getElementById('city-id-rbtn');